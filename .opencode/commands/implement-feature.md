---
description: Implement business logic from spec (both pipelines)
agent: implement-agent
---

Implement business logic for "$ARGUMENTS". Read `docs/MASTER-SPEC.md` for feature definitions. Detect pipeline from spec.

Load relevant context: Pipeline A (`api-patterns.md`), Pipeline B (`execution-standards.md`, `monetization-standards.md`).

Deliver per feature: types, schemas, API/workers, hooks, store, wired components, unit tests. All checks must pass.
