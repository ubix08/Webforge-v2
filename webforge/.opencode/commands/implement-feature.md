---
description: Implement business logic for a feature (Pipeline A)
agent: coder-agent
---

Implement business logic for "$ARGUMENTS". Read `docs/MASTER-SPEC.md` for the feature definition. Read `.opencode/context/api/api-patterns.md` for conventions.

Deliver per feature: types.ts, schemas.ts, api.ts, hooks.ts, store.ts, and wired components. Write Vitest unit tests for stores and hooks.
