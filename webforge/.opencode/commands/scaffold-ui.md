---
description: UI scaffold from existing spec (Pipeline A)
agent: ui-agent
---

Scaffold the UI for "$ARGUMENTS" from the existing spec at `docs/MASTER-SPEC.md` in the product directory.

Use React + Vite + TypeScript + shadcn/ui + Tailwind. Implement routing, layout, placeholder pages, loading/error/empty states.

Read `.opencode/context/design/ui-design-system.md` and `.opencode/context/stack/react-vite-standards.md` for conventions.
